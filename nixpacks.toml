[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build complete"']

[start]
cmd = 'cd ai-services && uvicorn api_server:app --host 0.0.0.0 --port $PORT'